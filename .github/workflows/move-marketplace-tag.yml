name: Move the Marketplace tag

on:
  workflow_call:

jobs:
  dist:
    name: Move the Marketplace tag
    if: github.ref_type == 'branch'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
