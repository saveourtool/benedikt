name: Create a binary distribution (full)

on:
  workflow_call:
    inputs:
      release-version:
        required: true
        type: string

jobs:
  dist:
    name: Binary distribution (full)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: |
          echo "RELEASE_VERSION=${{ inputs.release-version }}"
        shell: bash
